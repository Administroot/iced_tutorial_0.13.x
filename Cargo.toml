[package]
name = "iced-tutorial"
version = "0.1.0"
edition = "2021"

[dependencies]

iced = {version = "0.13.1", features = ["image", "svg", "tokio", "canvas"] }
tokio = {version = "1.42.0", features = ["time"]}
